name: YAML to HTML Sync - 자유와혁신 CMS 시스템 (DISABLED)

on:
  # 임시로 모든 트리거 비활성화
  workflow_dispatch:
    inputs:
      force_run:
        description: '강제 실행 (템플릿 준비 후)'
        required: false
        default: 'false'

# 기존 트리거들을 주석 처리
#  push:
#    branches: [ main ]
#    paths:
#      - 'content/**/*.yml'
#      - 'content/**/*.yaml'
#  pull_request:
#    branches: [ main ]
#    paths:
#      - 'content/**/*.yml'
#      - 'content/**/*.yaml'

env:
  NODE_VERSION: '18'
  PYTHON_VERSION: '3.9'

jobs:
  # 임시로 스킵
  skip-conversion:
    name: 템플릿 준비 중 - 변환 스킵
    runs-on: ubuntu-latest
    steps:
      - name: 알림
        run: |
          echo "⚠️ YAML → HTML 변환이 임시로 비활성화되었습니다"
          echo "📝 템플릿 파일들을 준비 중입니다"
          echo "�� 곧 다시 활성화될 예정입니다" 